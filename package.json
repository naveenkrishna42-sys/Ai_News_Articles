{
  "name": "ainewsss",
  "private": true,
  "version": "1.0.0",
  "description": "AI News Factory — static news site auto-built from HTML articles in /articles",
  "type": "module",
  "scripts": {
    "build": "node scripts/build-index.mjs",
    "import": "node scripts/import-articles.mjs",
    "news": "node scripts/auto-news.mjs",
    "news:test": "node scripts/auto-news.mjs --per-category 1 --max-total 6 --no-specials"
  }
}
